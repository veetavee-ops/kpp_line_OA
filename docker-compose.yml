services:
  app:
    image: veetavee/lineoa:${IMAGE_TAG:-latest}
    container_name: lineoa-app
    restart: unless-stopped
    ports:
      - "80:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
